# Giải nén notebook

```bash
gunzip -k notebook_drmd_lamda_unified_v14.ipynb.gz
```

Hoặc trên Linux/macOS:
```bash
gzip -dk notebook_drmd_lamda_unified_v14.ipynb.gz
```

File `.ipynb` đầy đủ (~276 KB) cũng sẽ được đẩy lên repo trong commit tiếp theo nếu tool cho phép.
